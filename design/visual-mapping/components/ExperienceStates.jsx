import React from 'react';
export function EmptyState({ illustration='◎', headline='Nothing here yet', description='When activity arrives, it will appear here.', action='Get started', onAction }) { return <div className="bb-panel" style={{padding:32,textAlign:'center',display:'grid',gap:10,placeItems:'center'}}><span aria-hidden="true" style={{fontSize:42,color:'var(--bb-accent)'}}>{illustration}</span><h2 style={{margin:0}}>{headline}</h2><p style={{margin:0,color:'var(--bb-text-muted)',maxWidth:360}}>{description}</p><button className="bb-focus" onClick={onAction} style={cta}>{action}</button></div>; }
export function LoadingState({ rows=4 }) { return <div aria-label="Loading content" style={{display:'grid',gap:10}}>{Array.from({length:rows},(_,i)=><div key={i} className="bb-skeleton" style={{height:i===0?84:48}}/>)}</div>; }
export function RecoveryState({ title='We could not complete that request', reason='The service may be temporarily unavailable.', retryLabel='Try again', onRetry }) { return <EmptyState illustration="!" headline={title} description={`${reason} You can retry now, wait a moment, or contact support if it continues.`} action={retryLabel} onAction={onRetry}/>; }
export function ActivityTimeline({ entries=[] }) { return <ol style={{listStyle:'none',padding:0,margin:0}}>{entries.map((entry,i)=><li key={`${entry.label}-${i}`} style={{display:'grid',gridTemplateColumns:'18px 1fr',gap:10,padding:'0 0 16px'}}><span style={{width:10,height:10,marginTop:5,borderRadius:'50%',background:'var(--bb-accent)'}}/><span><b>{entry.label}</b><small style={{display:'block',color:'var(--bb-text-muted)'}}>{entry.detail} · {entry.at}</small></span></li>)}</ol>; }
export const OrderTimeline=ActivityTimeline; const cta={border:0,borderRadius:8,padding:'0 18px',background:'var(--bb-accent)',color:'var(--bb-accent-ink)',fontWeight:800,cursor:'pointer'};
