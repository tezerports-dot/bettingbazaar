# domains/configuration — PLACEHOLDER

Not yet migrated. See ../README.md for the full domain map and status legend.

**Purpose:** Centralized business policy values (deposit ratios, limits, feature flags).

**Current source of truth (until migrated):** backend/models/systemConfig.model.js (exports SystemConfig and TokenRates)

Do not import from this folder — nothing lives here yet.
