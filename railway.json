{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci --legacy-peer-deps && cd user-panel && npm ci --legacy-peer-deps && npm run build && cd ../admin-panel && npm ci --legacy-peer-deps && npm run build && cd ../merchant-panel && npm ci --legacy-peer-deps && npm run build",
    "nixpacksPlan": {
      "variables": {
        "NIXPACKS_NO_CACHE": "1",
        "NIXPACKS_NODE_VERSION": "22"
      }
    }
  },
  "deploy": {
    "startCommand": "node backend/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60
  }
}
